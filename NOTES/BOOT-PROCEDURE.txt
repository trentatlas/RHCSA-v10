

FIRMWARE ( UEFI )
|
> BOOT DISK
| rescue disk ( ISO )
|
| > GRUB2 [ BOOT MENU ]
|   rescue kernel
|
| > KERNEL
| > INITRAMFS
|  init=/bin/bash
|
| > SYSTEMD
|  systemd.unit=emergency.target
|  systemd.unit=rescue.target
|  rd.break
   EMERGENCY MODE
   #chroot /sysroot

   -- chroot only avaible when kernel is re-generated using dracut using debug 
   https://access.redhat.com/solutions/7121729
   # dracut --force --add debug /boot/initramfs-$(uname -r).img $(uname -r)
   # systemctl reboot
     
| > default.target

